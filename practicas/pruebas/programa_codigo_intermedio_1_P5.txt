main {
	local {
		int n, curr;
	}

	cout "introduce numero: " ;
	cin n;
	cout " ", n, " == ";
	curr = 2;

	while ( curr <= n ) 
	{

		local {
			int d;
		}

		d = n / curr;

		if (d * curr == n) 
		{
			cout "* ", curr, " ";
			n = n / curr;
		}
		else
			curr = curr+1;
	}
	
	cout "\n";
}
